# Copy this to .env and fill in your values
PORT=3000
NODE_ENV=development

# PostgreSQL (Railway will provide this)
DATABASE_URL=postgresql://username:password@host:port/database

# API Keys
OPENROUTER_API_KEY=your_key_here

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your_random_secret_here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Security: Public self-service registration
# - Production default is DISABLED unless you explicitly enable it.
# - Set to true only if you really want anyone to be able to create accounts.
ALLOW_PUBLIC_SIGNUP=false

# Security: Invite-only registration (optional)
# If ALLOW_PUBLIC_SIGNUP=false and an invite code is set, users can register only with the invite code.
# NOTE: Do NOT commit real invite codes to git. Set them in your hosting provider env vars.
# You can use one code:
SIGNUP_INVITE_CODE=
# Or multiple codes, comma-separated:
SIGNUP_INVITE_CODES=

